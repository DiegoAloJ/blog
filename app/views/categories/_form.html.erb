<div class="container-fluid">
  <div class="row">
    <div class="col-md-8 ">
      <div class="jumbotron space-a">
        <form>
          <fieldset>
            <legend class="text-center space-title"><%= "#{title} Categoría" %></legend>
            <%= form_with(model: category, local: true) do |form| %>
              <% if category.errors.any? %>
                <div id="error_explanation">
                  <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>
                  <ul>
                    <% category.errors.full_messages.each do |message| %>
                      <li><%= message %></li>
                    <% end %>
                  </ul>
                </div>
              <% end %>
              <div class="form-group row">
                <%= form.label "Nombre: ", class:"col-md-4 col-form-label" %>
                <%= form.text_field :name, class:"col-md-6 form-control" %>
              </div>
              <div class="form-group row">
                <%= form.label "Color: ", class:"col-md-4 col-form-label" %>
                <%= form.text_field :color, class:"col-md-6 form-control" %>
              </div>
              <div class="form-group row">
                <%= form.submit "Aceptar", class:"btn btn-info" %>
              </div>
              <%= link_to 'Show', @category, class:"btn btn-info" %> 
              <%= link_to 'Back', categories_path, class:"btn btn-info" %>
            <% end %>
          </fieldset>
        </form>
      </div>
    </div>
  </div>
</div>